#pragma once

#include <cstdint>
#include <string>

//common
static const std::string DEVICE = "plughw:CARD=HID,DEV=0";
static const std::size_t DELAY_MS = 10;
static const std::size_t MAX_SIZE_QUEUE = 100;
